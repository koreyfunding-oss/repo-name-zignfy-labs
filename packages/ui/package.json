{
  "name": "@zignfy/ui",
  "version": "0.0.1",
  "description": "Shared UI components for Zignfy Labs micro-tools",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo",
    "lint": "tsc --noEmit",
    "test": "node --test dist/**/*.test.js"
  },
  "files": ["dist"],
  "keywords": ["ui", "components"],
  "license": "MIT"
}
